/**
 * `@tailor-platform/auth/client` is a package that provides a set of hooks and components
 * to handle user authentication in a standardized manner on client components.
 *
 * @packageDocumentation
 */
export { TailorAuthProvider } from "@client/provider";
export { useAuth, usePlatform, useSession } from "@client/hooks";
